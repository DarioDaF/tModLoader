--- src/TerrariaNetCore/Terraria/GameContent/UI/BigProgressBar/EaterOfWorldsProgressBar.cs
+++ src/tModLoader/Terraria/GameContent/UI/BigProgressBar/EaterOfWorldsProgressBar.cs
@@ -9,7 +_,7 @@
 		private float _lifePercentToShow;
 
 		public bool ValidateAndCollectNecessaryInfo(ref BigProgressBarInfo info) {
-			if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > 200)
+			if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > Main.maxNPCs) // OR==?
 				return false;
 
 			if (!Main.npc[info.npcIndexToAimAt].active && !TryFindingAnotherEOWPiece(ref info))
@@ -17,7 +_,7 @@
 
 			int eaterOfWorldsSegmentsCount = NPC.GetEaterOfWorldsSegmentsCount();
 			float num = 0f;
-			for (int i = 0; i < 200; i++) {
+			for (int i = 0; i < Main.maxNPCs; i++) {
 				NPC nPC = Main.npc[i];
 				if (nPC.active && nPC.type >= 13 && nPC.type <= 15)
 					num += (float)nPC.life / (float)nPC.lifeMax;
@@ -35,7 +_,7 @@
 		}
 
 		private bool TryFindingAnotherEOWPiece(ref BigProgressBarInfo info) {
-			for (int i = 0; i < 200; i++) {
+			for (int i = 0; i < Main.maxNPCs; i++) {
 				NPC nPC = Main.npc[i];
 				if (nPC.active && nPC.type >= 13 && nPC.type <= 15) {
 					info.npcIndexToAimAt = i;
