--- src/TerrariaNetCore/Terraria/GameContent/UI/BigProgressBar/GolemHeadProgressBar.cs
+++ src/tModLoader/Terraria/GameContent/UI/BigProgressBar/GolemHeadProgressBar.cs
@@ -19,7 +_,7 @@
 		}
 
 		public bool ValidateAndCollectNecessaryInfo(ref BigProgressBarInfo info) {
-			if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > 200)
+			if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > Main.maxNPCs) // OR==?
 				return false;
 
 			NPC nPC = Main.npc[info.npcIndexToAimAt];
@@ -32,7 +_,7 @@
 			_referenceDummy.SetDefaults(246, nPC.GetMatchingSpawnParams());
 			num += _referenceDummy.lifeMax;
 			float num2 = 0f;
-			for (int i = 0; i < 200; i++) {
+			for (int i = 0; i < Main.maxNPCs; i++) {
 				NPC nPC2 = Main.npc[i];
 				if (nPC2.active && ValidIds.Contains(nPC2.type))
 					num2 += (float)nPC2.life;
@@ -50,7 +_,7 @@
 		}
 
 		private bool TryFindingAnotherGolemPiece(ref BigProgressBarInfo info) {
-			for (int i = 0; i < 200; i++) {
+			for (int i = 0; i < Main.maxNPCs; i++) {
 				NPC nPC = Main.npc[i];
 				if (nPC.active && ValidIds.Contains(nPC.type)) {
 					info.npcIndexToAimAt = i;
